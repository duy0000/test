import React from "react";
import ReactDOM from "react-dom";

import SimpleMap from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SimpleMap />
  </React.StrictMode>
);
